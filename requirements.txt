gradio==5.34.1
tensorflow==2.19.0
tensorflow_hub==0.16.1
numpy==2.1.3
pillow==11.2.1
setuptools==80.9.0

# StyTr² needs PyTorch. The PyTorch index hosts CPU-only wheels (~200 MB)
# under a "+cpu" local version tag; without it, Linux pip would pull the
# CUDA-bundled wheel (~1.5 GB) and blow the container budget. macOS wheels on
# PyPI are already CPU-only, so they don't need the suffix.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.10.0+cpu ; sys_platform == "linux"
torch==2.10.0 ; sys_platform != "linux"
torchvision==0.25.0+cpu ; sys_platform == "linux"
torchvision==0.25.0 ; sys_platform != "linux"
